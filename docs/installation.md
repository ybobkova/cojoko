# Installation of Cojoko

## npm and grunt

The easiest way to install Cojoko (and recommended) is per npm. Cojoko uses grunt as a command line utilty to compile your Cojoko source files (model descriptions). 